import React from 'react'
import styles from './LeftSideBar.module.css';

export default function LeftSideBar() {
    return (
        <div className = { styles.container }>
            <div className = { styles.searchMenu }>
            <div className = { styles.header }> Menu </div> <br/>
            (Work-in-progress)
            </div>
        </div>
    )
}
